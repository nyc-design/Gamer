services:
  provisioner-api-test:
    build:
      context: ..
      dockerfile: tests/Dockerfile
    volumes:
      - ..:/app
    environment:
      - PYTHONPATH=/app
    working_dir: /app
    command: pytest